function hitungKelilingPersegi(panjang, lebar) {
     const keliling = 2 * (panjang + lebar);
     console.log(`Panjang = ${panjang}`);
     console.log(`Lebar = ${lebar}`);
     console.log(`Keliling = ${keliling}`);
   }
   
   module.exports = { hitungKelilingPersegi };
   